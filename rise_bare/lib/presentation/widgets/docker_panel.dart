import 'package:flutter/material.dart';

class DockerPanel extends StatelessWidget {
  const DockerPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Docker Panel - Coming soon'),
    );
  }
}
