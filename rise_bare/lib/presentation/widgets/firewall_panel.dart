import 'package:flutter/material.dart';

class FirewallPanel extends StatelessWidget {
  const FirewallPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Firewall Panel - Coming soon'),
    );
  }
}
