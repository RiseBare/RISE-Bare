import 'package:flutter/material.dart';

class HealthPanel extends StatelessWidget {
  const HealthPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Health Panel - Coming soon'),
    );
  }
}
