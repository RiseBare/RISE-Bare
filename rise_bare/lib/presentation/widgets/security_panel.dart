import 'package:flutter/material.dart';

class SecurityPanel extends StatelessWidget {
  const SecurityPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Security Panel - Coming soon'),
    );
  }
}
